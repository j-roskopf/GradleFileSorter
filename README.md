# GradleFileSorter

Sorts all Gradle files in an Android app according to an arbitrary and non-customizable process.

Replace `projectPath` in `main.js` with your desired project path.

It's only been tested on one project (NowInAndroid) so it probably doesn't work for your project :)